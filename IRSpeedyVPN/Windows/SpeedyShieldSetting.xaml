<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:controls="clr-namespace:IRSpeedyVPN.Controls" x:Class="IRSpeedyVPN.Windows.SpeedyShieldSetting" Title="تنظیمات شیلد" Width="420" Height="700" WindowStyle="None" AllowsTransparency="False" ResizeMode="NoResize" Background="{StaticResource WindowBackgroundBrush}" WindowStartupLocation="CenterOwner" ShowInTaskbar="False" FontFamily="{StaticResource AppFontFamily}" FlowDirection="RightToLeft" UseLayoutRounding="True" TextOptions.TextFormattingMode="Display" TextOptions.TextRenderingMode="ClearType" SizeToContent="Manual">
  <WindowChrome.WindowChrome>
    <WindowChrome CaptionHeight="0" CornerRadius="20" GlassFrameThickness="0" ResizeBorderThickness="0" UseAeroCaptionButtons="False"/>
  </WindowChrome.WindowChrome>
  <Grid ClipToBounds="True">
    <Grid.RowDefinitions>
      <RowDefinition Height="62"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="68"/>
    </Grid.RowDefinitions>
    <Border BorderBrush="#EEF0F4" BorderThickness="0,0,0,1">
      <Grid Margin="16,0" Background="Transparent" MouseLeftButtonDown="Header_DragMove">
        <StackPanel VerticalAlignment="Center">
          <TextBlock Text="تنظیمات شیلد" FontSize="16" Foreground="#141B33" TextAlignment="Right" FlowDirection="RightToLeft" FontWeight="ExtraBold" HorizontalAlignment="Center"/>
          <TextBlock Text="محافظت هوشمند در برابر تهدیدهای آنلاین" FontSize="10.5" Foreground="#9CA3B4" TextAlignment="Right" FlowDirection="RightToLeft" HorizontalAlignment="Center"/>
        </StackPanel>
        <Button Style="{StaticResource HandoffClose}" Click="Close_Click"/>
      </Grid>
    </Border>
    <Grid Grid.Row="1" Margin="16,16">
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="*"/>
          <RowDefinition Height="*"/>
          <RowDefinition Height="*"/>
          <RowDefinition Height="*"/>
          <RowDefinition Height="*"/>
          <RowDefinition Height="*"/>
          <RowDefinition Height="*"/>
          <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
          <Border Height="60" CornerRadius="18" Background="White" BorderBrush="#ECEEF3" BorderThickness="1" Padding="14,0">
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="42"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
              </Grid.ColumnDefinitions>
              <controls:ServiceToggle x:Name="GoogleAdsToggle" AutomationProperties.Name="GoogleAdsToggle" Grid.Column="2"/>
              <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0">
                <TextBlock Text="تبلیغات گوگل" FontSize="14.5" Foreground="#1B2033" TextAlignment="Right" FlowDirection="RightToLeft" FontWeight="Bold"/>
                <TextBlock FontSize="10.5" TextAlignment="Right" FlowDirection="RightToLeft">
                  <TextBlock.Style>
                    <Style TargetType="TextBlock">
                      <Setter Property="Text" Value="این محتوا آزاد است"/>
                      <Setter Property="Foreground" Value="#EF333B"/>
                      <Style.Triggers>
                        <DataTrigger Binding="{Binding IsOn, ElementName=GoogleAdsToggle}" Value="True">
                          <Setter Property="Text" Value="این محتوا مسدود می‌شود"/>
                          <Setter Property="Foreground" Value="#17AB77"/>
                        </DataTrigger>
                      </Style.Triggers>
                    </Style>
                  </TextBlock.Style>
                </TextBlock>
              </StackPanel>
              <Border Grid.Column="0" Width="36" Height="36" CornerRadius="13" Background="#1AF97316">
                <Image Source="{StaticResource HandoffAdsImage}" Width="21" Height="21" FlowDirection="LeftToRight"/>
              </Border>
            </Grid>
          </Border>
        </Grid>
        <Grid Grid.Row="1">
          <Border Height="60" CornerRadius="18" Background="White" BorderBrush="#ECEEF3" BorderThickness="1" Padding="14,0">
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="42"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
              </Grid.ColumnDefinitions>
              <controls:ServiceToggle x:Name="TrackersToggle" AutomationProperties.Name="TrackersToggle" Grid.Column="2"/>
              <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0">
                <TextBlock Text="تبلیغات و ردیاب" FontSize="14.5" Foreground="#1B2033" TextAlignment="Right" FlowDirection="RightToLeft" FontWeight="Bold"/>
                <TextBlock FontSize="10.5" TextAlignment="Right" FlowDirection="RightToLeft">
                  <TextBlock.Style>
                    <Style TargetType="TextBlock">
                      <Setter Property="Text" Value="این محتوا آزاد است"/>
                      <Setter Property="Foreground" Value="#EF333B"/>
                      <Style.Triggers>
                        <DataTrigger Binding="{Binding IsOn, ElementName=TrackersToggle}" Value="True">
                          <Setter Property="Text" Value="این محتوا مسدود می‌شود"/>
                          <Setter Property="Foreground" Value="#17AB77"/>
                        </DataTrigger>
                      </Style.Triggers>
                    </Style>
                  </TextBlock.Style>
                </TextBlock>
              </StackPanel>
              <Border Grid.Column="0" Width="36" Height="36" CornerRadius="13" Background="#1A0EA5E9">
                <Image Source="{StaticResource HandoffTrackerImage}" Width="21" Height="21" FlowDirection="LeftToRight"/>
              </Border>
            </Grid>
          </Border>
        </Grid>
        <Grid Grid.Row="2">
          <Border Height="60" CornerRadius="18" Background="White" BorderBrush="#ECEEF3" BorderThickness="1" Padding="14,0">
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="42"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
              </Grid.ColumnDefinitions>
              <controls:ServiceToggle x:Name="AdultToggle" AutomationProperties.Name="AdultToggle" Grid.Column="2"/>
              <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0">
                <TextBlock Text="محتوای +۱۸" FontSize="14.5" Foreground="#1B2033" TextAlignment="Right" FlowDirection="RightToLeft" FontWeight="Bold"/>
                <TextBlock FontSize="10.5" TextAlignment="Right" FlowDirection="RightToLeft">
                  <TextBlock.Style>
                    <Style TargetType="TextBlock">
                      <Setter Property="Text" Value="این محتوا آزاد است"/>
                      <Setter Property="Foreground" Value="#EF333B"/>
                      <Style.Triggers>
                        <DataTrigger Binding="{Binding IsOn, ElementName=AdultToggle}" Value="True">
                          <Setter Property="Text" Value="این محتوا مسدود می‌شود"/>
                          <Setter Property="Foreground" Value="#17AB77"/>
                        </DataTrigger>
                      </Style.Triggers>
                    </Style>
                  </TextBlock.Style>
                </TextBlock>
              </StackPanel>
              <Border Grid.Column="0" Width="36" Height="36" CornerRadius="13" Background="#1ADC2626">
                <Image Source="{StaticResource HandoffAdultImage}" Width="21" Height="21" FlowDirection="LeftToRight"/>
              </Border>
            </Grid>
          </Border>
        </Grid>
        <Grid Grid.Row="3">
          <Border Height="60" CornerRadius="18" Background="White" BorderBrush="#ECEEF3" BorderThickness="1" Padding="14,0">
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="42"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
              </Grid.ColumnDefinitions>
              <controls:ServiceToggle x:Name="SocialsToggle" AutomationProperties.Name="SocialsToggle" Grid.Column="2"/>
              <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0">
                <TextBlock Text="شبکه‌های اجتماعی" FontSize="14.5" Foreground="#1B2033" TextAlignment="Right" FlowDirection="RightToLeft" FontWeight="Bold"/>
                <TextBlock FontSize="10.5" TextAlignment="Right" FlowDirection="RightToLeft">
                  <TextBlock.Style>
                    <Style TargetType="TextBlock">
                      <Setter Property="Text" Value="این محتوا آزاد است"/>
                      <Setter Property="Foreground" Value="#EF333B"/>
                      <Style.Triggers>
                        <DataTrigger Binding="{Binding IsOn, ElementName=SocialsToggle}" Value="True">
                          <Setter Property="Text" Value="این محتوا مسدود می‌شود"/>
                          <Setter Property="Foreground" Value="#17AB77"/>
                        </DataTrigger>
                      </Style.Triggers>
                    </Style>
                  </TextBlock.Style>
                </TextBlock>
              </StackPanel>
              <Border Grid.Column="0" Width="36" Height="36" CornerRadius="13" Background="#1A7C3AED">
                <Image Source="{StaticResource HandoffSocialImage}" Width="21" Height="21" FlowDirection="LeftToRight"/>
              </Border>
            </Grid>
          </Border>
        </Grid>
        <Grid Grid.Row="4">
          <Border Height="60" CornerRadius="18" Background="White" BorderBrush="#ECEEF3" BorderThickness="1" Padding="14,0">
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="42"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
              </Grid.ColumnDefinitions>
              <controls:ServiceToggle x:Name="CryptoToggle" AutomationProperties.Name="CryptoToggle" Grid.Column="2"/>
              <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0">
                <TextBlock Text="رمزارز" FontSize="14.5" Foreground="#1B2033" TextAlignment="Right" FlowDirection="RightToLeft" FontWeight="Bold"/>
                <TextBlock FontSize="10.5" TextAlignment="Right" FlowDirection="RightToLeft">
                  <TextBlock.Style>
                    <Style TargetType="TextBlock">
                      <Setter Property="Text" Value="این محتوا آزاد است"/>
                      <Setter Property="Foreground" Value="#EF333B"/>
                      <Style.Triggers>
                        <DataTrigger Binding="{Binding IsOn, ElementName=CryptoToggle}" Value="True">
                          <Setter Property="Text" Value="این محتوا مسدود می‌شود"/>
                          <Setter Property="Foreground" Value="#17AB77"/>
                        </DataTrigger>
                      </Style.Triggers>
                    </Style>
                  </TextBlock.Style>
                </TextBlock>
              </StackPanel>
              <Border Grid.Column="0" Width="36" Height="36" CornerRadius="13" Background="#1ACA8A04">
                <Image Source="{StaticResource HandoffCryptoImage}" Width="21" Height="21" FlowDirection="LeftToRight"/>
              </Border>
            </Grid>
          </Border>
        </Grid>
        <Grid Grid.Row="5">
          <Border Height="60" CornerRadius="18" Background="White" BorderBrush="#ECEEF3" BorderThickness="1" Padding="14,0">
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="42"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
              </Grid.ColumnDefinitions>
              <controls:ServiceToggle x:Name="GamblingToggle" AutomationProperties.Name="GamblingToggle" Grid.Column="2"/>
              <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0">
                <TextBlock Text="قمار" FontSize="14.5" Foreground="#1B2033" TextAlignment="Right" FlowDirection="RightToLeft" FontWeight="Bold"/>
                <TextBlock FontSize="10.5" TextAlignment="Right" FlowDirection="RightToLeft">
                  <TextBlock.Style>
                    <Style TargetType="TextBlock">
                      <Setter Property="Text" Value="این محتوا آزاد است"/>
                      <Setter Property="Foreground" Value="#EF333B"/>
                      <Style.Triggers>
                        <DataTrigger Binding="{Binding IsOn, ElementName=GamblingToggle}" Value="True">
                          <Setter Property="Text" Value="این محتوا مسدود می‌شود"/>
                          <Setter Property="Foreground" Value="#17AB77"/>
                        </DataTrigger>
                      </Style.Triggers>
                    </Style>
                  </TextBlock.Style>
                </TextBlock>
              </StackPanel>
              <Border Grid.Column="0" Width="36" Height="36" CornerRadius="13" Background="#1ADB2777">
                <Image Source="{StaticResource HandoffGamblingImage}" Width="21" Height="21" FlowDirection="LeftToRight"/>
              </Border>
            </Grid>
          </Border>
        </Grid>
        <Grid Grid.Row="6">
          <Border Height="60" CornerRadius="18" Background="White" BorderBrush="#ECEEF3" BorderThickness="1" Padding="14,0">
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="42"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
              </Grid.ColumnDefinitions>
              <controls:ServiceToggle x:Name="PhishingToggle" AutomationProperties.Name="PhishingToggle" Grid.Column="2"/>
              <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0">
                <TextBlock Text="فیشینگ" FontSize="14.5" Foreground="#1B2033" TextAlignment="Right" FlowDirection="RightToLeft" FontWeight="Bold"/>
                <TextBlock FontSize="10.5" TextAlignment="Right" FlowDirection="RightToLeft">
                  <TextBlock.Style>
                    <Style TargetType="TextBlock">
                      <Setter Property="Text" Value="این محتوا آزاد است"/>
                      <Setter Property="Foreground" Value="#EF333B"/>
                      <Style.Triggers>
                        <DataTrigger Binding="{Binding IsOn, ElementName=PhishingToggle}" Value="True">
                          <Setter Property="Text" Value="این محتوا مسدود می‌شود"/>
                          <Setter Property="Foreground" Value="#17AB77"/>
                        </DataTrigger>
                      </Style.Triggers>
                    </Style>
                  </TextBlock.Style>
                </TextBlock>
              </StackPanel>
              <Border Grid.Column="0" Width="36" Height="36" CornerRadius="13" Background="#1A0D9488">
                <Image Source="{StaticResource HandoffPhishingImage}" Width="21" Height="21" FlowDirection="LeftToRight"/>
              </Border>
            </Grid>
          </Border>
        </Grid>
        <Grid Grid.Row="7">
          <Border Height="60" CornerRadius="18" Background="White" BorderBrush="#ECEEF3" BorderThickness="1" Padding="14,0">
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="42"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
              </Grid.ColumnDefinitions>
              <controls:ServiceToggle x:Name="MalwareToggle" AutomationProperties.Name="MalwareToggle" Grid.Column="2"/>
              <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0">
                <TextBlock Text="بدافزار" FontSize="14.5" Foreground="#1B2033" TextAlignment="Right" FlowDirection="RightToLeft" FontWeight="Bold"/>
                <TextBlock FontSize="10.5" TextAlignment="Right" FlowDirection="RightToLeft">
                  <TextBlock.Style>
                    <Style TargetType="TextBlock">
                      <Setter Property="Text" Value="این محتوا آزاد است"/>
                      <Setter Property="Foreground" Value="#EF333B"/>
                      <Style.Triggers>
                        <DataTrigger Binding="{Binding IsOn, ElementName=MalwareToggle}" Value="True">
                          <Setter Property="Text" Value="این محتوا مسدود می‌شود"/>
                          <Setter Property="Foreground" Value="#17AB77"/>
                        </DataTrigger>
                      </Style.Triggers>
                    </Style>
                  </TextBlock.Style>
                </TextBlock>
              </StackPanel>
              <Border Grid.Column="0" Width="36" Height="36" CornerRadius="13" Background="#1A475569">
                <Image Source="{StaticResource HandoffMalwareImage}" Width="21" Height="21" FlowDirection="LeftToRight"/>
              </Border>
            </Grid>
          </Border>
        </Grid>
      </Grid>
    </Grid>
    <Button Grid.Row="2" Margin="16,0,16,18" Height="50" Style="{StaticResource DesignActionButton}" Content="تایید و ذخیره" Click="btnOK_Click"/>
  </Grid>
</Window>
