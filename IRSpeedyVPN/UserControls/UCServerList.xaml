<UserControl x:Class="IRSpeedyVPN.UserControls.UCServerList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IRSpeedyVPN.Components.ServerListControl"
             mc:Ignorable="d" 
             Loaded="UserControl_Loaded" 
             IsVisibleChanged="UserControl_IsVisibleChanged">

    <Grid>
        <Border Background="Transparent" CornerRadius="5">
            <Grid Margin="0,30,0,0" x:Name="mainGrd">
                <StackPanel Margin="0">
                    <StackPanel Margin="40,0" Height="160">

                        <Grid Margin="4">
                            <TextBlock FontSize="17" FontWeight="Bold" Text="سرویس"
                                       HorizontalAlignment="Right" VerticalAlignment="Center"/>
                            <ComboBox Width="230" x:Name="cmbService"
                                      FontFamily="Arial" FontSize="14" FontWeight="Bold"
                                      FlowDirection="RightToLeft" HorizontalAlignment="Left"
                                      Background="White" BorderBrush="{x:Null}"
                                      SelectionChanged="cmbService_SelectionChanged"/>
                        </Grid>

                        <Grid x:Name="gProtocol" Margin="4">
                            <TextBlock FontSize="17" FontWeight="Bold" Text="پروتکل"
                                       HorizontalAlignment="Right" VerticalAlignment="Center"/>
                            <ComboBox Width="230" x:Name="cmbProtocol"
                                      FontFamily="Arial" FontSize="14" FontWeight="Bold"
                                      FlowDirection="RightToLeft" HorizontalAlignment="Left"
                                      Background="White" BorderBrush="{x:Null}"
                                      SelectionChanged="cmbProtocol_SelectionChanged"/>
                        </Grid>

                        <!-- ─── Country = the new custom picker ─── -->
                        <Grid Margin="4">
                            <TextBlock FontSize="17" FontWeight="Bold" Text="کشور"
                                       HorizontalAlignment="Right" VerticalAlignment="Center"/>
                            <local:ServerCountryPicker x:Name="countryPicker"
                                                         Width="230"
                                                         HorizontalAlignment="Left"/>
                        </Grid>

                        <Grid Margin="0,10"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button x:Name="btnConnect" Margin="10,0,10,10"
                                Style="{StaticResource RoundCorner}"
                                Click="btnConnect_Click"
                                Content="اتصال" IsCancel="True" FontSize="20"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>