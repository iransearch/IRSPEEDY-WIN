<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:IRSpeedyVPN.Themes">

    <Style x:Key="TextBoxButton" BasedOn="{x:Null}" TargetType="{x:Type TextBox}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="1"/>
        <Setter Property="AllowDrop" Value="true"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="ScrollViewer.PanningMode" Value="VerticalFirst"/>
        <Setter Property="Stylus.IsFlicksEnabled" Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TextBox}">
                    <TextBox x:Name="txt" Cursor="Hand" FontSize="{TemplateBinding FontSize}" BorderThickness="{TemplateBinding BorderThickness}" IsReadOnly="True" Text="{TemplateBinding Text}" Background="{x:Null}" Foreground="{TemplateBinding Foreground}" BorderBrush="{x:Null}"  />
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="FontSize"  Value="24"/>
                        </Trigger>
                        <EventTrigger RoutedEvent="MouseEnter">
                            <BeginStoryboard Name="FocusTrueStoryboard">
                                <Storyboard>
                                    <DoubleAnimation
            Storyboard.TargetName="txt" 
            Storyboard.TargetProperty="(TextBlock.Opacity)"
            From="1.0" To="0.6" Duration="0:0:.50" 
          />
                                </Storyboard>

                            </BeginStoryboard>
                            
                        </EventTrigger>
                        <EventTrigger RoutedEvent="MouseLeave">
                            <BeginStoryboard >
                                <Storyboard>
                                    <DoubleAnimation
            Storyboard.TargetName="txt" 
            Storyboard.TargetProperty="(TextBlock.Opacity)"
            To="1.0" Duration="0:0:1" 
             />
                                </Storyboard>

                            </BeginStoryboard>

                        </EventTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
                
            </Setter.Value>
        </Setter>
        
    </Style>

</ResourceDictionary>