<UserControl x:Class="IRSpeedyVPN.Controls.ServiceToggle" x:Name="root"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 Width="46" Height="26" FlowDirection="LeftToRight" Focusable="False">
 <Button Click="Toggle_Click" Cursor="Hand" AutomationProperties.Name="{Binding Path=(AutomationProperties.Name), ElementName=root}">
  <Button.Template><ControlTemplate TargetType="Button">
   <Border x:Name="track" Background="{StaticResource ToggleOffBrush}" CornerRadius="13" Padding="3">
    <Border x:Name="thumb" Width="20" Height="20" CornerRadius="10" Background="White" HorizontalAlignment="Left">
     <Border.Effect><DropShadowEffect Color="#101828" BlurRadius="4" ShadowDepth="2" Opacity="0.25"/></Border.Effect>
     <Border.RenderTransform><TranslateTransform X="0"/></Border.RenderTransform>
    </Border>
   </Border>
   <ControlTemplate.Triggers>
    <DataTrigger Binding="{Binding IsOn, ElementName=root}" Value="True">
     <Setter TargetName="track" Property="Background" Value="{StaticResource ToggleOnBrush}"/>
     <DataTrigger.EnterActions><BeginStoryboard><Storyboard>
      <DoubleAnimation Storyboard.TargetName="thumb" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)" To="20" Duration="0:0:0.12"/>
     </Storyboard></BeginStoryboard></DataTrigger.EnterActions>
     <DataTrigger.ExitActions><BeginStoryboard><Storyboard>
      <DoubleAnimation Storyboard.TargetName="thumb" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)" To="0" Duration="0:0:0.12"/>
     </Storyboard></BeginStoryboard></DataTrigger.ExitActions>
    </DataTrigger>
    <Trigger Property="IsEnabled" Value="False"><Setter TargetName="track" Property="Opacity" Value="0.5"/></Trigger>
    <Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="track" Property="BorderBrush" Value="{StaticResource AccentBrush}"/><Setter TargetName="track" Property="BorderThickness" Value="1"/><Setter TargetName="track" Property="Padding" Value="2"/></Trigger>
   </ControlTemplate.Triggers>
  </ControlTemplate></Button.Template>
 </Button>
</UserControl>
